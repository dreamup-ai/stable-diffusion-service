services:
  stable-diffusion-service:
    image: public.ecr.aws/i0t3i1w9/stable-diffusion-server:lykon-dreamshaper
    # runtime: nvidia
    network_mode: host
    build:
      context: ./
      dockerfile: Dockerfile.base
    ports:
      - "1111:1111"
    environment:
      - HOST=*
      - PORT=1111
      - MODEL_DIR=/models
      - MODELS=Lykon/DreamShaper,prompthero/openjourney-v4
    # volumes:
      # - /models:/models
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    